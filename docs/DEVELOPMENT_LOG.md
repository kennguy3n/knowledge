# Development Log

Per-session changelog for the Knowledge substrate. For the live
phase-status summary and per-phase deliverable checklists see
[`PROGRESS.md`](../PROGRESS.md).

## Changelog

| Date | Change |
|------|--------|
| 2026-05-08 | **End-to-end demo crate + Phase 4 connector line-items + per-phase benchmark report.** New `crates/demo/` workspace member ships a single binary (`cargo run --bin demo`) that exercises the full Phase 1 → Phase 12 substrate pipeline against a 51-message synthetic dataset spanning user / channel / domain / tenant scopes and all three evidence storage paths (inline / body-table / ring-buffer). The demo touches only public crate APIs, builds an `EvidenceStore`, runs the `LexiconExtractor` + `CompositeClassifier`, drives the `MemoryStateMachine` through Candidate → Reinforced → Consolidated → Canonical, persists a 60-node concept graph via `PersistentConceptGraph`, runs the channel → domain → tenant synthesis chain, seeds a Zanzibar permission graph and verifies reachability, exercises hybrid KEM + AEAD + cryptographic forgetting, renders `PortableConceptProfile`s through the export plane (`ConceptsOnly` + `WithSummaries` views) with `PolicySimulator` preview, runs the agent contract Proposed → UnderReview → Promoted lifecycle (auto + manual + TTL paths), drives the `ContradictionDetector` + `GraphTraversal` + `GoTExecutor` + `CommunityDetector` + `QueryPlanner` over the union of canonical nodes / edges from every scope, exercises four connectors (Google Drive, Jira, Slack, Email — Gmail + Graph) against fixture pages with initial + incremental + webhook flows, and queries the audit log by scope / action type / time range. Every phase emits a per-phase `PhaseReport` (timing + summary stats + notes) into `results/demo_results.md`, plus a `Benchmarks` section with per-operation timings (`evidence_ingest_per_message`, `observation_extraction_per_message`, concept-graph operations, synthesis pipeline, export rendering). The run logs **196 PASS / 0 FAIL** assertions across the twelve phases. Integration tests in `crates/demo/tests/end_to_end.rs` re-spawn the demo binary in a tempdir and verify it (1) exits cleanly, (2) writes a well-formed `results/demo_results.md` with all 12 phase headings + summary / benchmarks / assertion sections, (3) records zero `FAIL` rows, (4) reconciles the stdout assertion summary with the in-file totals, and (5) emits parseable timing data for every phase + a populated benchmarks table. Phase 4 PROGRESS.md gains explicit line items for the Slack and Email connectors (already shipped in the prior drop, now correctly tracked); Phase 4 status moves from `100% (12 of 12)` to `100% (14 of 14)`. PHASES.md gains the matching Slack + Email bullets; ARCHITECTURE.md updates the connectors lib doc-comment from "seven concrete connectors" to "nine concrete connectors" and the system-overview tree gains the new `crates/demo/` member. README.md gains a `Demo` section. `cargo fmt --all -- --check` + `cargo clippy --all-targets --all-features -- -D warnings` + `cargo test --all` are clean. |
| 2026-05-08 | **Substrate hardening: 6 production bug fixes, 6 targeted regression tests, FFI integration suite, inference-router integration suite.** No new deliverables — every Phase 0–7 line item remains at 100%; this drop only fixes correctness defects discovered on top of the post-Phase-7 substrate and tightens the test surface around the FFI and inference-router crates. **Bug 1 (memory-leak in retrieval)**: `crates/evidence_store/src/retrieval.rs::rerank_with_embeddings` no longer leaks the embedder error string via `Box::leak` — `EvidenceError` gains an `Embedding(String)` variant in `crates/evidence_store/src/error.rs`, and the failure path now propagates `EvidenceError::Embedding(format!(...))` cleanly. **Bug 2 (trailing-session boundary)**: `crates/memory_manager/src/episodic.rs::SessionDetector::detect` no longer leaks the previous session's close reason onto the trailing session — the trailing session is never explicitly ended by any input, so its boundary is now always `SessionBoundary::TimeGap`. The two pre-existing detector tests that asserted the leaked-boundary behaviour (`detector_splits_on_explicit_marker`, `detector_splits_on_scope_change`) are realigned to assert the *closing* session's boundary (where the trigger actually belongs) plus the new trailing-session invariant. **Bug 3 (`TeeWorkerLifecycle::Idle` reachability)**: `crates/synthesis_engine/src/tee_worker.rs` adds an internal `Lifecycle::Idle` state wired into `as_public()`, `exit_synthesizing()` now transitions `Synthesizing → Idle` (instead of silently flipping `active = false` and returning to `Attested`), and the next dispatch transitions `Idle → Attested` again. The `Unattested → Attesting → Attested → Synthesizing → Idle` state machine the module doc advertises is now actually observable through `lifecycle()`. **Bug 4 (semantic component wired)**: `crates/evidence_store/src/retrieval.rs::HybridRetriever::search_hybrid` now embeds the query and each candidate body through the configured `EmbeddingModel`, uses `cosine_distance` to compute the semantic similarity, and falls back to `0.0` (with the new `EvidenceError::Embedding` surfaced for unrecoverable errors) when the model is `None` or fails. The stale `// stubbed at 0.0 until XLM-R ships` comment is gone. **Bug 5 (synthesis window stuck `InProgress`)**: `crates/synthesis_engine/src/managed_endpoint.rs::synthesize_domain` and `synthesize_tenant` now `windows.mark_failed(handle.window_id)` on dispatch error before propagating `EndpointError`, so a transient HTTP / endpoint failure no longer leaves the window pinned in `InProgress`. **Bug 6 (mock client variant loss on clone)**: `EndpointError` now derives `Clone`, so `MockBehaviour::Error(err)` clones the underlying variant faithfully (e.g. `Timeout(_)` round-trips as `Timeout(_)` instead of being collapsed into `Endpoint("...")`); test fakes that take `EndpointError` as a sequence parameter now reproduce the exact error shape downstream code is expected to handle. **Tests — 6 regressions**: each bug ships an inline regression test in the same module so a future revert breaks CI — `rerank_with_embeddings` failure path produces `EvidenceError::Embedding` (not `Schema`); trailing session after explicit close has `SessionBoundary::TimeGap`; `TeeWorker::lifecycle()` returns `Idle` post-synthesis before transitioning back to `Attested`; `search_hybrid` with a non-stub embedding model produces non-zero `vector_score` values; dispatch failure transitions the synthesis window to `Failed` (not stuck in `InProgress`); `MockHttpClient::failing(EndpointError::Timeout(...))` returns `EndpointError::Timeout` (not `EndpointError::Endpoint`). **Tests — FFI integration suite**: new `crates/ffi/tests/ffi_integration_tests.rs` covers the full FFI surface (`ingest_message`, `query`, `get_evidence`, `get_user_memory`, `pin`, `unpin`, `forget`, `list_memories`, `run_decay_sweep`, `get_channel_memory`, `trigger_synthesis`, `generate_keypair`, `encrypt`, `decrypt`), the FFI error-mapping layer, the `ingest → query → get_evidence` round-trip, and the `generate_keypair → encrypt → decrypt` crypto round-trip. **Tests — inference-router integration suite**: new `crates/inference_router/tests/router_integration_tests.rs` covers adapter priority (`MlxAdapter → LlamaCppAdapter → FallbackAdapter`), `DeviceTier` gating (`Low` rejects SLM tasks), warm-up + 60s idle-unload lifecycle, every `InferenceTask` variant (`TagImportance` / `ExtractEntities` / `PromoteObservation` / `SynthSummary` / `SynthConcept` / `AdjudicateContradiction`) routes to the right adapter, `FallbackAdapter` succeeds on classification tasks but errors on synthesis tasks, and every `RouterError` variant is reachable via a deterministic input. New tests bring the total to **1072 passing tests** (up from 1036); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. All phases remain at 100%. |
| 2026-05-08 | **Phase 0 SLM importance classifier + platform bindings, Phase 1 episodic memory + XLM-R embeddings, Phase 4 Slack + email connectors, Phase 7 SPHINCS+ co-signer + confidential-compute TEE worker.** Closed out the last four Phase 0 line items, the last two Phase 1 line items, the Slack + email connectors, and the last two Phase 7 line items in a single drop, taking every phase from 0 \u2192 7 to 100% complete. **Phase 0 inference router**: new crate `crates/inference_router/` ships `InferenceAdapter` trait (`probe` / `generate(task_tag, prompt, grammar)` / `is_available` / `supports`), `InferenceTask::{TagImportance, ExtractEntities, PromoteObservation, SynthSummary, SynthConcept, AdjudicateContradiction}` with associated tag strings + prompt templates + GBNF grammars, `InferenceRouter` that bootstraps adapters in priority order (MLX \u2192 LlamaCpp \u2192 Fallback), respects `DeviceTier::{Low, Medium, High}`, handles warm-up + 60s idle-unload, `RouterConfig`, `RouterError::{Unavailable, Generation, NoAdapter, Tier, ProbeFailed, Internal}`, `MlxAdapter` skeleton (Apple Silicon `probe`), `LlamaCppAdapter` skeleton (HTTP loopback `probe`), `FallbackAdapter` (encoder-only \u2014 succeeds on classification tasks via lexicon, errors on synthesis tasks). **Phase 0 SLM importance classifier**: `crates/evidence_store/src/classifier.rs` extended with `SlmClassifier` (dispatches to `InferenceRouter` with the `TagImportance` task; grammar constrains output to `{\"class\": \"critical|important|useful|noise\", \"confidence\": 0.0\u20131.0}`; falls back to `LexiconClassifier` on `RouterError::Unavailable`) and `CompositeClassifier` (lexicon \u2192 SLM chain; lexicon runs first as a cheap pre-filter, SLM only runs when lexicon confidence is below threshold). **Phase 0 + 1 platform bindings**: new crate `crates/ffi/` ships the UniFFI surface for iOS / Android (evidence: `ingest_message` / `query` / `get_evidence`; memory manager: `get_user_memory` / `pin` / `unpin` / `forget` / `list_memories(scope_id, filter)` / `run_decay_sweep`; synthesis: `get_channel_memory` / `trigger_synthesis`; crypto: `generate_keypair` / `encrypt(scope_id, plaintext)` / `decrypt(scope_id, ciphertext)`); FFI-safe wrapper types (String IDs, simplified enums) in `types.rs`; FFI error mapping in `error.rs`; `uniffi.toml` config. New crate `crates/napi/` ships the N-API addon skeleton with the same surface and an `init(config_json)` entrypoint plus N-API exception mapping. **Phase 1 episodic memory**: `crates/memory_manager/src/episodic.rs` ships `EpisodicSummary` (id, scope_id, session_id, summary_text, key_observations, time_range, created_at, state, retention_score), `SessionBoundary` detection (time gap > 30 min, explicit user action, topic-shift signal), `Summarizer` trait, `SlmSummarizer` (dispatches to `InferenceRouter` with `SynthSummary`), `StubSummarizer` (concatenated observation text), `EpisodicStore` CRUD integrated with the decay state machine (episodic summaries start as `Candidate`, promote to `Reinforced` on retrieval), and `SessionDetector` over evidence-timestamp streams. **Phase 1 XLM-R embeddings**: `crates/evidence_store/src/embeddings.rs` ships `EmbeddingModel` trait (`embed` / `embed_batch` / `dimension`), `OnnxEmbeddingAdapter` skeleton holding `(model_path, quantization: INT8/INT4, dimension: 768)` with `probe` checking ONNX runtime availability behind a mockable `OnnxRuntime` trait, `StubEmbeddingModel` (zero-vectors of correct dimension), and `cosine_distance`; `crates/evidence_store/src/retrieval.rs::HybridRetriever` accepts an optional `Box<dyn EmbeddingModel>` and replaces the hardcoded `0.0` semantic score with real cosine similarity when an embedding model is provided. **Phase 7 SPHINCS+ co-signer**: `crates/crypto/src/sphincs.rs` ships `SphincsPlusSigner` and `SphincsPlusVerifier` (FIPS 205 `slh-dsa-shake-128f`) implementing both the substrate-wide `ProvenanceSigner` and `SignerBackend` traits, `SphincsPlusEncodedKeypair` / `SphincsPlusEncodedVerifyingKey` transport types with mismatched-keypair rejection via `CryptoError::ProvenanceVerification`, and `CoSigner` wrapping both `MlDsa65Signer` + `SphincsPlusSigner` to produce dual signatures for archival group ops (each verified independently). **Phase 7 confidential-compute TEE worker**: `crates/synthesis_engine/src/tee_worker.rs` ships `TeeWorkerConfig` (platform, expected_measurement, synthesis_key_pair, scope_bindings), `TeeWorker` implementing `SynthesisEngine`, `TeeWorkerLifecycle::{Unattested, Attesting, Attested, Synthesizing, Idle}` state machine, `attest()` (generates the attestation report, binds the synthesizer key, produces an `AttestationAuditEntry`), `synthesize_domain` / `synthesize_tenant` (verifies attestation is current, decrypts inputs inside the simulated enclave, runs synthesis, encrypts outputs, publishes), and `MockTeeRuntime` simulating TEE behaviour with `mock_attestation_report` from `crates/crypto/src/attestation.rs` for the full attest \u2192 synthesize \u2192 verify lifecycle (including expiry rejection and wrong-measurement rejection). **Phase 4 Slack connector**: `crates/connectors/src/slack.rs` ships `SlackConnector` implementing `Connector`: OAuth2 with `channels:history` + `channels:read` + `files:read` scopes, `conversations.list` + `conversations.history` for initial sync, `conversations.history` with `oldest` Unix-timestamp cursor for incremental sync, Slack Events API subscription with URL-verification challenge handling for `message` / `file_shared` / `channel_archive` event types, and event-envelope parsing into `ConnectorEvent::{DocumentCreated, DocumentUpdated, DocumentDeleted}` (with full unknown-event-type rejection). **Phase 4 email connector**: `crates/connectors/src/email.rs` ships `EmailConnector` with `EmailProvider::{Gmail, MicrosoftGraph}` for provider-specific logic: OAuth2 for both providers, `messages.list` (Gmail) / `/me/messages` (Graph) for initial sync with cursor-based pagination, `after`-date cursor filtering for incremental sync, Gmail Cloud Pub/Sub push notifications and Graph `/subscriptions` (`changeType: created`) for webhooks, and parsing of Gmail push notifications + Graph change notifications into `ConnectorEvent::DocumentCreated`. `ConnectorKind` extended with `Slack` and `Email` variants in `crates/connector_framework/src/config.rs`. Workspace `Cargo.toml` extended with `crates/inference_router`, `crates/ffi`, `crates/napi`. New tests bring the total to **1036 passing tests** (up from 829); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. With this drop every phase is at 100% completion. |
| 2026-05-08 | **Phase 7 post-quantum hardening, Phase 2 MLS, Phase 3 managed endpoint, Phase 6 visualization, Phase 7 quality metrics + red-team tests.** Closed out the remaining Phase-2 / Phase-3 / Phase-6 line items and seven of the nine Phase-7 deliverables in a single drop. **Phase 7 ML-DSA-65 provenance signer**: `MlDsa65Signer` and `MlDsa65Verifier` in `crates/crypto/src/signer_backend.rs` use the RustCrypto `ml-dsa` (FIPS 204) crate, implement the substrate-wide `ProvenanceSigner` trait alongside a new `SignerBackend` trait (`sign_bytes` / `verify_bytes`), expose encoded keypair / verifying key types for transport, and reject mismatched-keypair decoding via `CryptoError::ProvenanceVerification`; `TestSigner` (HMAC-SHA256) is preserved as a test-only fallback. **Phase 7 cryptographic forgetting**: `crates/crypto/src/forgetting.rs` ships `ScopeId` / `EpochId` newtypes, `ScopeDek` / `EpochDek` with `Drop`-time `zeroize` of the underlying key bytes, `DekRegistry` (in-memory `(scope_id, epoch)`-indexed registry that preserves tombstones so `is_scope_forgotten` / `is_epoch_forgotten` are stable after destruction), `destroy_scope_dek` / `destroy_epoch_dek` returning `Vec<KeyDestructionEvent>` for audit fan-out, the `KeyDestructionAuditor` trait, and `record_key_destructions` for piping events into `audit_service`. **Phase 7 policy-driven epoch rotation**: same module ships `EpochRotationTrigger::{TimeElapsed, SizeExceeded, PolicyForced}`, `EpochRotationPolicy` (default and configurable), `EpochInfo`, `EpochManager` (one current epoch per scope, plus historical cold epochs), `current_epoch` / `list_epochs` / `force_rotate` / `record_bytes` (size-trigger) / `tick` (time-trigger), and the `EpochKeySource` trait with a `DeterministicEpochKeySource` for tests. **Phase 7 hybrid classical + PQ enforcement**: `crates/crypto/src/hybrid_enforcement.rs` ships `HybridMode::{ClassicalOnly, HybridTransition, PostQuantumOnly}`, `CryptoPolicy`, `KemPrimitives` (hybrid / classical-only / pq-only), `KeyExchangeDirection` / `KeyExchangeOutcome`, `KeyExchangeAudit`, `KeyExchangeAuditor` trait, `enforce_hybrid_kem` validation, `enforce_hybrid_kem_encap` / `enforce_hybrid_kem_decap` policy-checked wrappers around the existing `hybrid_kem_encap` / `hybrid_kem_decap`, and `InMemoryKeyExchangeAuditor` for tests. **Phase 2 MLS group keying**: `crates/crypto/src/mls.rs` ships a skeletal MLS keying data model — `MlsGroup` (group_id, epoch, members, tree shape), `LeafKeyPackage` (hybrid X25519 + ML-KEM-768 leaf KEM + ML-DSA-65 verifying key), `MlsCommit` (ML-DSA-65-signed), `MlsWelcome`, `GroupKeySchedule`, `add_member` / `remove_member` / `process_commit`. Documented as the key-schedule + group-state surface only; the full RFC 9420 wire protocol lives in [`kennguy3n/openmls`](https://github.com/kennguy3n/openmls). **Phase 6 concept graph visualization**: `crates/concept_graph/src/visualization.rs` ships `GraphView` (nodes / edges / scope_filter / depth / layout_hints / truncation), `ViewFilter` (scope_ids, node_states, relation_types, max_depth, max_nodes), `NodeVisual` (id, label, state, scope_id, position_hint, connections_count), `EdgeVisual`, `PositionHint`, `TruncationReason`, `explore_from` / `subgraph_for_scope` / `neighborhood` / `search_nodes`, the `ScopeAccess` permission gating trait (`AllowAllScopes` for tests, `AllowedScopeSet` for explicit allow-lists, plus a blanket impl over closures `Fn(ScopeId) -> bool`) compatible with `permission_service::check_permission`. **Phase 3 managed AI endpoint**: `crates/synthesis_engine/src/managed_endpoint.rs` ships `EndpointConfig` (url, api_key_ref, model_id, max_tokens, timeout, optional grammar), `InputObjectRef`, `SynthesisRequest`, `SynthesisResponse`, `EndpointError` (timeout / rate-limit / invalid-response / transport / auth / serialisation), the `HttpClient` trait, `MockHttpClient` with `MockBehaviour::{Echo, Fixed, Failing, Sequence}`, `HttpManagedEndpointSynthesizer` implementing the existing `SynthesisEngine` trait (`synthesize_domain`, `synthesize_tenant`) by building a grammar-constrained request, dispatching it through the trait, validating the response, and packaging the output as a `DomainMemoryObject` / `TenantMemoryObject`. **Phase 7 memory quality metrics**: `crates/memory_manager/src/metrics.rs` ships `RetentionPrecisionTracker` (computes `retrieved_retained / total_retained` precision), `ContradictionDetectionRate` (computes `caught / total_pairs`), `DecayTuningMetrics` (per-sweep promoted / archived / deleted counts plus aggregate ratios), `MemoryQualityReport` (snapshot of all three with timestamps), and `MetricsCollector` that hooks into the decay state machine and retrieval paths. **Phase 7 red-team privacy and prompt-injection tests**: `crates/evidence_store/tests/privacy_redteam.rs` ships 11 tests covering scope-isolation (evidence written under scope A is not readable under scope B's key), forgotten-scope recovery (after `destroy_scope_dek` no record is recoverable from the in-memory registry), cross-scope dedup (BLAKE3 content hash collision between scopes does not leak across the storage path), ring-buffer overwrite (noise-class messages are physically overwritten, not merely tombstoned), append-only canonical promotion (`agent_contract` lifecycle blocks direct writes), permission-service boundary (viewer cannot promote, member cannot admin), agent boundary (proposals must be reviewed; agents cannot write canonical memory directly), provenance integrity (tampered `entity_id` and tampered signature both fail verification under `TestSigner`), and key-material handling (the `Drop` impl on `ScopeDek` / `EpochDek` zeroes the key bytes before deallocation). `crates/synthesis_pipeline/tests/privacy_redteam.rs` ships 10 tests covering AAD-bound scope / window / object-id replay rejection, wrong-key rejection, ciphertext / nonce-byte tampering detection (Poly1305 catches both), prompt-injection containment (a malicious payload remains opaque to the schema layer), AAD smuggling rejection, distinct keys producing independent ciphertexts under a shared nonce, and inner-routing mismatch with matched outer AEAD (defence-in-depth). **Phase 7 attestation reports**: `crates/crypto/src/attestation.rs` ships `TeePlatform::{IntelTdx, AmdSevSnp, NitroEnclaves, Mock}`, `AttestationReport` (report_id, platform, measurement, report_data, signature, created_at), `AttestationBinding` (binds a synthesizer public key to a report), `AttestationAuditEntry::{success, failure}` for audit-service linkage, `verify_attestation`, `bind_synthesizer_key`, `mock_attestation_report` for tests, plus a full mock-TEE flow test. New tests bring the total to **829 passing tests** (up from 706 on `main`); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. The two Phase-7 line items still open are the full RFC 9420 / SPHINCS+ MLS extensions and the concrete confidential-compute TEE worker (both gated on the upstream `openmls` integration and the TDX / SEV-SNP runtime work, respectively). |
| 2026-05-08 | **Phase 4 connectors + Phase 6 GoT / Community summaries.** Closed out the seven remaining Phase-4 vendor connectors, the Phase-4 connector-specific integration tests, and the two remaining Phase-6 reasoning items (Graph-of-Thought and community summaries). New crate `crates/connectors/`: implements the `Connector` trait from `connector_framework` for seven vendors as in-process fakes that model each vendor's API contract over fixture JSON (no real HTTP — the actual HTTP client lands when the Go gateway does). `GoogleDriveConnector` (`crates/connectors/src/google_drive.rs`) walks `files.list` for the initial sync, paginates the Changes API with `startPageToken` cursors for incremental sync, subscribes resource channels with HMAC-secret-bound TTLs, and parses Drive push notification payloads (`add` / `update` / `remove` / `change` resource states) into `ConnectorEvent::{DocumentCreated, DocumentUpdated, DocumentDeleted, PermissionChanged}`. `OneDriveConnector` (`crates/connectors/src/onedrive.rs`) drives `/drive/root/delta` with `@odata.deltaLink` cursor handoff for incremental sync, subscribes Microsoft Graph subscriptions with `clientState` secrets, and parses `value[].changeType` payloads. `NotionConnector` (`crates/connectors/src/notion.rs`) issues `/search` for the initial pass, paginates `/databases/{id}/query` with a `last_edited_time >= cursor` filter for the incremental pass, and stubs `subscribe_webhook` to return a polling-only `ConnectorError` (Notion has no native webhooks); `handle_webhook_event` therefore rejects events as `Webhook("polling-only")`. `JiraConnector` (`crates/connectors/src/jira.rs`) drives `/search` (JQL) with `ORDER BY created` for initial sync, `updated >= cursor_timestamp` for incremental, subscribes Jira webhooks bound by HMAC secret, and parses Jira `webhookEvent` payloads (`jira:issue_created` / `jira:issue_updated` / `jira:issue_deleted` / `permission_changed`). `ConfluenceConnector` (`crates/connectors/src/confluence.rs`) walks `/content?expand=body.storage` for initial sync, filters by `lastModified` for incremental, subscribes Confluence webhooks, and parses `page_created` / `page_updated` / `page_removed` events. `FigmaConnector` (`crates/connectors/src/figma.rs`) reads `/files/{key}` and `/files/{key}/components` for design-system component extraction, advances incremental via the `version` field as cursor, subscribes Figma webhooks, and parses `FILE_UPDATE` / `FILE_DELETE` / `LIBRARY_PUBLISH` events. `HubSpotConnector` (`crates/connectors/src/hubspot.rs`) pages through `/crm/v3/objects/{type}` for contacts / companies / deals / notes on the initial sync with cursor-based pagination, filters by `lastModifiedDate` for incremental, subscribes HubSpot webhook subscriptions, and parses `subscriptionType` payloads (`contact.creation` / `contact.propertyChange` / `contact.deletion` / etc.). All seven connectors emit canonical `ConnectorEvent` values keyed by stable `SourceDocumentId`s and route OAuth2 tokens through `connector_framework`'s `OAuth2TokenVault`. The `ConnectorKind` enum in `crates/connector_framework/src/config.rs` was extended with the missing `Figma` and `HubSpot` variants and corresponding `as_str()` arms returning `"figma"` and `"hubspot"`. Connector-specific integration tests live in `crates/connectors/tests/` with vendor fixtures in `crates/connectors/tests/fixtures/`: `google_drive_tests.rs`, `onedrive_tests.rs`, `notion_tests.rs`, `jira_tests.rs`, `confluence_tests.rs`, `figma_tests.rs`, `hubspot_tests.rs` — each test file constructs the connector with fixture-backed pages, calls `authenticate` and asserts token shape and OAuth scopes, calls `initial_sync` and asserts emitted events + cursor, calls `incremental_sync` with the cursor and asserts delta events, calls `subscribe_webhook` and asserts subscription shape, calls `handle_webhook_event` with vendor-specific JSON and asserts the correct `ConnectorEvent` variant, and exercises error paths (invalid JSON, missing required fields, unknown event types, expired tokens). Phase 6 Graph-of-Thought ships in `crates/reasoning_engine/src/graph_of_thought.rs`: `ThoughtNode` (id, content, `ThoughtType::{Hypothesis, Evidence, Conclusion, Question, Refinement}`, confidence, parent_ids, scope_id), `ThoughtEdge::{Supports, Contradicts, Refines, Derives, Aggregates}`, `ThoughtGraph` (in-memory directed thought graph with insertion-order iteration + `connect_child` that requires every parent already exist), `GoTQuery` (question, scope, `max_depth`, `max_branches`, `max_nodes`), `GoTStrategy::{BreadthFirst, DepthFirst, BestFirst, Iterative}`, `Expander` trait with `StaticExpander` (deterministic test fake) and `GraphExpander` (delegates to `crates/reasoning_engine/src/traversal.rs::GraphTraversal` for factual grounding), `GoTPlan` (root + sub-questions), `ScoredPath`, `GoTResult` (best_path, all_paths, confidence, reasoning_trace, budget_exhausted, succeeded), and `GoTExecutor` orchestrating `plan` → `expand` → `evaluate` → `execute` plus `record_trace` that persists completed runs into `crates/reasoning_engine/src/workflow.rs::WorkflowMemory` as `WorkflowTrace`s. Phase 6 community summaries ship in `crates/reasoning_engine/src/community.rs`: `CommunityDetector` runs a connected-component scan over a scope-filtered, relation-filtered subgraph of canonical-only concept nodes; `Community` carries member node ids, scope ids, label, level, child ids; `CommunityHierarchy::build` recursively merges sibling leaves whose scope sets overlap to build a multi-level GraphRAG-style hierarchy and exposes `leaves()` / `at_level()` / `level_count()`; `CommunitySummaryGenerator` collects canonical concepts in each community, groups every member-internal edge by `RelationType`, and renders a structured `CommunitySummary` (community_id, level, summary_text, key_concepts, key_relations grouped by relation type, generated_at, scope_ids); `CommunityQueryRouter::route` tokenises a free-form query, scores each summary by overlap against `key_concepts` + `summary_text`, applies the `permission_service::check_permission` visibility filter so a caller only sees summaries whose constituent scopes they have `viewer` (or higher) access to, sorts by descending score, and returns the top-`limit` matches. Both modules are re-exported from `crates/reasoning_engine/src/lib.rs`. Workspace `Cargo.toml` extended with `crates/connectors`. `cargo test --all` reports **697 tests passing** (up from 609 on `main`); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. The single remaining Phase-6 line item is the Kanvas-style concept-graph visualization. |
| 2026-05-08 | **Phase 4 + Phase 6, first delivery.** Landed seven Phase-4 and Phase-6 deliverables across two new crates and two existing-crate extensions. New crate `crates/connector_framework/`: the `Connector` trait (`authenticate`, `initial_sync`, `incremental_sync`, `subscribe_webhook`, `handle_webhook_event`), `OAuth2TokenVault` with HKDF-derived `SecretToken` wrappers and a `TokenRefresher` trait that handles expiring-token refresh with configurable skew, `SyncState` (Full / Incremental sync modes with cursor + last-sync-time tracking), `WebhookSubscription` metadata + `WebhookSignatureVerifier` HMAC-SHA256 verifier, `ConnectorConfig` (connector type, scope binding, auth config, sync interval), `ConnectorInstance` runtime state, and the `ConnectorEvent` enum (`DocumentCreated / DocumentUpdated / DocumentDeleted / PermissionChanged`). Phase 4 channel-scoped attachment ships in `crates/connector_framework/src/attachment.rs`: `ConnectorAttachment` binds a `ConnectorInstanceId` to a `ScopeId`, `AttachmentRegistry` enforces one-connector-per-source-per-scope, and `AttachmentService` integrates with `permission_service` (only `admin` / `editor` on the scope can attach / detach connectors). ACL sync ships in `crates/connector_framework/src/acl_sync.rs`: `SourcePermission` (source_user_id, source_document_id, permission level), `PermissionMapping` configurable lookup from source levels to substrate relations (viewer / member / editor / admin), and `AclSyncEngine::sync_permissions` upserts and revokes relation tuples in the permission service idempotently. New crate `crates/reasoning_engine/`: `ContradictionDetector` (`crates/reasoning_engine/src/contradiction.rs`) scans the concept graph for canonical-pair opposing claims and emits explicit `Contradicts` edges with detected_at + confidence + dual evidence_refs, plus an `AdjudicationWorkflow` state machine (`Detected → UnderReview → Resolved(winner, loser)` or `Resolved(both_valid_in_context)`); `DriftDetector` (`crates/reasoning_engine/src/drift.rs`) flags canonical claims whose evidence base has been superseded or weakened with `DriftMarker`s; `GraphTraversal` (`crates/reasoning_engine/src/traversal.rs`) — typed-edge BFS over the concept graph with `TraversalBudget` (max_hops / max_nodes_visited / max_time_ms / max_edges_per_hop), `TraversalQuery` (start_node, optional target, edge-type / scope filters, direction), `TraversalResult` (paths + visited + reasoning trace + completed_at), `PathScorer` (per-relation weights + depth penalty), and both targeted (A → B) + exploratory (A → ?) modes; `QueryPlanner` (`crates/reasoning_engine/src/planner.rs`) — `RetrievalMode::{Summary, Fts, SemanticVector, GraphTraversal, RawEvidence}` with `cost_rank`, `QueryClassifier` (point lookup / relational / temporal / holistic), `PlannerHeuristics` (per-class fallback chains, default chain), and `QueryPlanner::execute` that stops at the first successful step; `WorkflowMemory` (`crates/reasoning_engine/src/workflow.rs`) — `WorkflowTrace` (query + plan + steps + result + duration + scope), `WorkflowPattern` (template query + common steps + success rate), `PatternMatcher` (Jaccard-token similarity), and `TraceRecorder` for step-by-step recording. Phase 4 document observation pipeline ships in `crates/observation_engine/src/document.rs`: `DocumentRef` source identifier, `DocumentKind::{PlainText, Markdown, Json}`, `ChunkMetadata` (citation-grade: source doc ref, chunk index, byte / char offsets), `DocumentChunker` trait + `SlidingWindowChunker` (configurable window size + overlap; correct on Unicode boundaries), and `DocumentObservationPipeline<H, E, C>` chaining chunking → importance tagging (reusing `ImportanceClassifier`) → extraction (reusing `LexiconExtractor`) with metadata propagation onto every observation. Phase 4 citation rendering ships in `crates/observation_engine/src/citation.rs`: `Citation` (id, source_url, source_type, document_id, optional section_ref, chunk_range, last_verified_at), `CitationSourceType` (GoogleDrive / OneDrive / Notion / Jira / Confluence / Internal / Other), `CitationFormat` (Markdown / Json / InlineRef), `CitationRenderer::render`, `CitationRegistry` (URL reverse-indexed in-memory store with stale-detection helper). Phase 6 incremental graph updates ship in `crates/concept_graph/src/incremental.rs`: `IncrementalUpdateEngine` recomputes only touched branches when an observation is promoted / superseded; `AffectedSubgraph` BFS-walks direct neighbours + transitive `derived_from` dependents up to a configurable max depth; `RecomputeScope` filters to the minimal set of nodes whose state actually depends on the change; `UpdatePropagation` carries the bookkeeping result; `ChangeEvent::{NodePromoted, NodeSuperseded, NodeContradicted, EdgeAdded, EdgeRemoved}` is the full mutation surface; tests cover single-node promotion, supersession cascades, contradiction propagation, depth limits, edge add / remove, and no-op when a change has no neighbours. Workspace `Cargo.toml` extended with the two new members. Test coverage added: 43 connector-framework tests across token vault, sync state transitions, webhook event parsing, attachment registry CRUD + permission checks, ACL mapping + idempotent upsert + revocation; 34 reasoning-engine tests across contradiction detection / adjudication, drift markers, single-hop / multi-hop / exploratory traversal, budget exhaustion, scope + typed-edge filtering, point-lookup / relational / temporal / holistic planning, fallback chains, plan execution, trace recording, pattern extraction + matching; 8 document-pipeline tests covering empty / single / multi-chunk / Unicode / metadata propagation; 8 citation tests covering registry round-trip + URL indexing + render formats + stale detection; 8 incremental-update tests covering promotion / supersession / contradiction propagation + depth limits + edge ops + no-op cases. The whole workspace now ships with `cargo test --all` reporting **609 tests passing** (up from 507 on `main`); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. Phase-4 line items still open are the seven concrete connectors (Google Drive / OneDrive / Notion / Jira / Confluence / Figma / HubSpot) and the connector-specific integration tests; Phase-6 line items still open are the concept-graph visualization, Graph-of-Thought reasoning, and community summaries. |
| 2026-05-07 | **Phase 5 — Portable Concept Profiles & Export, first delivery.** Landed all eight Phase-5 deliverables across two new crates and three audit-service extensions. New crate `crates/agent_contract`: agent proposal data model with `AgentProposal<T>` generic carrier, four typed proposal payloads (`ObservationProposal / ConceptProposal / RelationProposal / SummaryProposal`), `AgentIdentity` (agent_id, name, model_name, model_version, optional skill_id + recipe_id), reuse of canonical `ProvenanceBundle` from `crypto` and `SensitivityClass` from `memory_manager` (no type duplication), schema validation enforcing confidence in `[0.0, 1.0]`, ≥1 evidence ref, non-nil scope, non-empty agent identity, and TTL > 0 if present (`crates/agent_contract/src/schema.rs`), and the four-state lifecycle machine `Proposed → UnderReview → Promoted/Rejected` with an `AutoPromotionPolicy` (min_confidence, min_corroboration, max_sensitivity, require_human_for_critical), an in-memory `ProposalStore`, and a `promote_to_canonical` adaptor that emits `CanonicalArtifact::{Observation,Concept,Relation,Summary}` ready for substrate insertion (`crates/agent_contract/src/lifecycle.rs`). New crate `crates/export_plane`: portable concept profile data model (`PortableConceptProfile`, `ApprovedConcept`, `ApprovedSummary`, `ExportView` with `ConceptsOnly / WithSummaries / WithEvidencePack` variants, `EvidencePack`, `ReasoningRef`, `ExportConstraint::{MaxConcepts, MaxAge, ScopeRestriction, SensitivityCeiling}`) per `PROPOSAL.md` §3.5; `ExportPolicy` engine (`crates/export_plane/src/policy.rs`) enforcing `allow_raw_evidence` + sensitivity-ceiling + scope whitelist + max_concepts + time_window + provenance-required least-privilege defaults, with a `PolicyEngine::evaluate` returning approved + rejected + warnings; `ExportControlRegistry` with deny-by-default per-concept / per-summary / per-workflow controls including time-bound and scope-bound enforcement (`crates/export_plane/src/controls.rs`); `PolicySimulator::simulate` (`crates/export_plane/src/simulator.rs`) that returns `SimulationResult { included_concepts, excluded_concepts, included_summaries, excluded_summaries, would_include_evidence, total_export_size_estimate, warnings }` *without* mutating any inputs or producing a real export; and `ConceptApprovalWorkflow` (`crates/export_plane/src/approval.rs`) bridging `concept_graph` canonical concepts to `ApprovedConcept`s — only nodes whose state is `NodeState::Canonical` and whose registry entry has `exportable: true` may be approved, with `revoke_approval` and per-scope `list_approved`. Extended `crates/audit_service`: added `ExportRendered`, `ExportSimulated`, `AgentProposalSubmitted`, `AgentProposalPromoted`, `AgentProposalRejected` to `AuditActionType` and an `ExportProfile` `TargetType` variant (`crates/audit_service/src/entry.rs`); new helpers module `crates/audit_service/src/helpers.rs` with `log_export`, `log_export_simulated`, `log_proposal_submitted`, `log_proposal_promoted`, `log_proposal_rejected`. Workspace `Cargo.toml` extended with the two new members. Test coverage added: 13 schema + lifecycle tests + 9 cross-crate end-to-end proposal tests (`crates/agent_contract/tests/e2e_proposal_tests.rs`) covering full observation / concept / relation / summary lifecycles with audit trail, manual + auto promotion, agent-cannot-write-canonical-directly rejection, schema-validation rejection, expired-TTL auto-rejection, rejection-with-reason audit, and critical-sensitivity-requires-human under default policy; 13 policy-engine tests, 16 export-control tests, 7 simulator tests, 7 approval-workflow tests, 8 profile / view tests, plus 8 cross-crate end-to-end export tests (`crates/export_plane/tests/e2e_export_tests.rs`) covering the full `concept_graph → approval → simulation → render → audit` pipeline, deny-by-default of unapproved concepts, default-policy blocking of `Critical` concepts, raw-evidence blocking when any concept is `Critical`, candidate-state rejection, revocation removing concept from approved set, time-window filtering, and a full `agent_contract → concept_graph → export_plane → audit_service` integration that wires `submit → promote → approve → simulate → render → audit` and asserts the four-event audit trail (`AgentProposalSubmitted → AgentProposalPromoted → ExportSimulated → ExportRendered`). The whole workspace now ships with `cargo test --all` reporting **464 tests passing** (up from 355 on `main`); `cargo fmt --all -- --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. The single remaining Phase-5 line item is the production export bundle signing path, which depends on the Phase-7 ML-DSA-65 signer and is intentionally still tracked under Phase 7. |
| 2026-05-07 | **Phase 3 status refresh.** Bumped the Phase 3 row in the overall-status summary from `~80% (8 of 10)` to `~90% (9 of 10)` to match the actual checkbox state under §`Phase 3 — Domain & Tenant Memory (B2B server)`. The Phase-3 checklist itself already reflects the delivered surface from PR #7 (10 of 11 boxes ticked once the bonus append-only audit log is counted; against the original 10-deliverable Phase-3 list, 9 of 10 are checked). The single remaining item — `Managed AI endpoint as synthesizer for B2B channels / domains` — is an intentional Phase-4 deferral, gated on the Go gateway in front of `crates/synthesis_engine/` (`ManagedEndpointSynthesizer` skeleton already in place). No code or test changes; `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all` (355 tests) all pass against the existing `main`. |
| 2026-05-07 | **Phase 3 — Domain & Tenant Memory, first delivery.** Landed eight of the ten Phase-3 deliverables across four new crates and three extensions, plus the append-only audit-log surface called out in `ARCHITECTURE.md` §4.1. New crate `crates/permission_service`: Zanzibar-style relation tuples (`ObjectRef`, `Relation`, `SubjectRef`) with the ten object types from `ARCHITECTURE.md` §6 (Tenant / Domain / Channel / User / Device / Concept / Summary / Workflow / ExportProfile / Agent), the seven-relation namespace (`owner`, `admin`, `editor`, `member`, `synthesizer`, `viewer`, `proposer`) with default inheritance (`owner ⇒ admin ⇒ editor ⇒ member ⇒ viewer`), an in-memory `TupleStore`, and a `check_permission` reachability query that walks both direct tuples and userset rewrites. New crate `crates/tenant_service`: `Tenant` / `TenantConfig` / `TenantMember` data model, the `Active / Suspended / Deleted` lifecycle state machine with key-destruction reference on delete, role-based member provisioning, and config validation (storage caps, sub-minute synthesis windows). New crate `crates/synthesis_engine`: server-side synthesis-engine skeleton with the `SynthesisEngine` trait (`synthesize_domain`, `synthesize_tenant`) and a `ManagedEndpointSynthesizer` deterministic stub that the future managed-AI endpoint will replace. New crate `crates/audit_service`: append-only `AuditLog` with `AuditEntryBuilder`, `AuditQuery` (scope / action / actor / time-range filters), and the eight `AuditActionType`s (`CanonicalPromotion / Export / AgentProposal / PolicyChange / MemberProvisioned / MemberRemoved / TenantLifecycle / KeyDestruction`). Extended `crates/memory_manager` with `DomainMemoryObject` (workstreams, dependencies, risks, procedures, registered channel scopes, archive-on-decay sweep) and `TenantMemoryObject` (canonical policies, product taxonomy, stable org knowledge, admitted approved-document refs; tenant items default to `SensitivityClass::Critical` with no passive decay — only explicit deprecation per `PROPOSAL.md` §4.3). Extended `crates/synthesis_pipeline` with `crates/synthesis_pipeline/src/hierarchy.rs`: `ChannelOutput` (only constructible from `ChannelRecap` synthesis objects), `DomainOutput` (only from `DomainSummary`), `DomainSynthesisInput` (only constructible from `ChannelOutput`s registered on a `DomainMemoryObject`; raw `ChannelMemoryObject` rejected at the type level), `TenantSynthesisInput` (only constructible from `DomainOutput`s registered on a `TenantMemoryObject` plus `ApprovedDocument`s admitted to that tenant; channel-tier objects rejected at the type level), `WindowScopeTier` (`Channel / Domain / Tenant`), and a `HierarchyEnforcedWindowManager` blanket impl on `SynthesisWindowManager` whose `validate_domain_input` / `validate_tenant_input` refuse cross-tier admission. Extended `crates/concept_graph` with `PersistentConceptGraph`: SQLCipher-backed wrapper over the Phase-2 in-memory adjacency list, `concept_nodes` + `concept_edges` schema with plaintext lifecycle / relation tags for scope-filtered queries, and per-scope AEAD encryption (`scope:{uuid}:concept:v1` HKDF context) of the JSON-encoded node/edge payloads with `(scope_id, id)` bound into the AAD. Test coverage added: 18 permission-service tests across tuple CRUD, reachability over inheritance chains, transitive userset rewrites, and negative cases; 10 tenant-service tests covering the lifecycle state machine + member provisioning + config validation; 6 concept-graph persistence tests covering encrypted round-trip, scope filtering, supersession persistence, and reload-after-restart; 8 synthesis-engine tests covering the trait contract + window completion + hierarchy validation; 9 audit-service tests covering append-only invariants, scope / action / actor / time filters, and chronological ordering; 10 domain-memory tests + 11 tenant-memory tests covering CRUD, lifecycle, archive sweeps, no-passive-decay enforcement, and explicit deprecation; and a 460-line end-to-end test (`crates/synthesis_engine/tests/hierarchy_e2e.rs`) that wires `memory_manager`, `synthesis_pipeline`, `synthesis_engine`, `concept_graph`, `permission_service`, `audit_service`, and `crypto` together: it builds the channel → domain → tenant synthesis chain, signs a provenance bundle at every tier, persists the tenant summary as a canonical concept through the encrypted graph, appends audit entries with sequence + scope filtering, and asserts the cross-tier permission gates (outsider blocked at tenant, channel member blocked at tenant admin, admin reaches every tier through the userset-rewrite chain). The two remaining Phase-3 items — the Go-side gateway in front of the `synthesis_engine` and the production managed-AI endpoint adapter — are intentionally still open and tracked under Phase 4. `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all` all pass against the new surface. |
| 2026-05-07 | **Phase 2 — Channel Memory, first delivery.** Landed seven of the nine Phase-2 deliverables across four new crates and three extensions. New crate `crates/concept_graph`: sparse typed concept graph per `PROPOSAL.md` §3.3 / `ARCHITECTURE.md` §2.1 with `ConceptNode` (id, label, definition, scope_id, state, created_at, updated_at, metadata), seven typed relations (`IsA`, `PartOf`, `DecidedBy`, `Supersedes`, `Contradicts`, `DerivedFrom`, `AssignedTo`), in-memory `HashMap`-backed adjacency, scope-aware add / remove / supersede / mark-contradiction / typed-edge traversal. New crate `crates/synthesis_pipeline`: per-scope `SynthesisWindowManager` with `Pending / InProgress / Complete / Failed` status machine, typed `SynthesisObject` (`EpisodicSummary / ChannelRecap / DomainSummary / TenantSummary`), GBNF grammar types (`ImportanceTag`, `EntityList`, `ObservationRow`, `SummaryBundle`) per `ARCHITECTURE.md` §3.5, `SynthesisPipeline` trait with a `NoOpSynthesizer` for end-to-end wiring tests, encrypted `publish_synthesis_object` / `consume_synthesis_object` with `(scope_id, window_id, object_id)` bound into the AEAD AAD, and a `SynthesizerElection` skeleton (tier / battery > 20 % / heartbeat-TTL eligibility, election by tier-then-recency, voluntary `step_down`, re-election on offline). Extended `crates/memory_manager` with `ChannelMemoryObject` (recap, decisions, open questions, active tasks; reuses `MemoryObject` for individual items; archive-on-decay sweep). Extended `crates/crypto` with the PROV data model (`ProvenanceBundle`, `ProvenanceSigner` trait, `TestSigner` via HMAC-SHA256; ML-DSA-65 signer slot reserved for Phase 7). Fleshed out `crates/sync_engine` from a stub into a real add-wins observed-remove CRDT (`AddWinsSet<T>`) plus an append-only `OpLog<T>` with `Add / Remove / Supersede` ops and `merge_logs` producing deterministic merged state. Extended `crates/observation_engine` with a `ChannelPromotionPolicy` (min importance class, min corroboration count, max noise ratio) and hardened the `LexiconExtractor` with URL / email / date-time-ref / numeric-ref / question detection (sentences ending in `?` or starting with an interrogative word are now `Question` observations). Updated `Cargo.toml` workspace members and `[lints]` blocks across the new crates. Test coverage added: 11 promotion tests, 22 extraction edge-case tests (empty / whitespace-only / >10 KB / URL-only / mention-only / Unicode + emoji / multi-line / mixed-case / dedup / interrogative + question-mark detection), 14 graph tests, 20+ synthesis-pipeline tests across window / object / schema / pipeline / publish / election, 12 CRDT tests covering add-wins semantics + merge commutativity + idempotency + supersession contradictions, 10 channel-memory tests covering CRUD + lifecycle + decay-sweep, and 8 provenance tests covering signing round-trip + tamper detection + wrong-key rejection. The two remaining Phase-2 items (MLS group keying for the shared-channel keying path and the ML-DSA-65 signer for the provenance plane) depend on Phase-7 PQ hardening and are intentionally still open. |
| 2026-05-07 | **Phase 1 — Personal Memory, first delivery.** Two evidence-plane bug fixes that were holding back cross-scope reads and ring-buffer telemetry: (1) `body_store` rows are now encrypted with a scope-independent key derived from the master key with the context label `b"body_store:v1"`, so a body inserted from scope A and dedup-shared into scope B can be decrypted from either evidence row; (2) `ring_buffer.created_at` now carries Unix epoch seconds (matching `RingBufferEntry.created_at`'s documented unit and the `evidence` table's `created_at`), instead of microseconds. New crate `crates/memory_manager`: the seven-state decay machine (`Candidate / Reinforced / Consolidated / Canonical / Superseded / Archived / Deleted`) per `ARCHITECTURE.md` §7, a `MemoryObject` carrying retention + sensitivity (`Critical / Important / Useful / Noise`) bookkeeping, weighted retention scoring (pinning, retrieval frequency, cross-source corroboration, contradiction signals, age, non-use) per `PROPOSAL.md` §4.2 with per-class half-lives and a hard pinning floor (`>=0.9`), a decay sweep (low-score Candidate → Archived; TTL-elapsed Superseded → Archived), a bounded TTL-evicting `WorkingMemory` window, a `UserMemoryObject` CRUD wrapper (read / pin / unpin / forget / list / decay sweep) that auto-promotes pinned Candidates, and a `PrivacyStrip` + `SynthesisOutput<T>` pair whose only public constructor demands a strip — encoding compute location, model name + version, egress bytes, and data scope per Phase-1 §1. New crate `crates/observation_engine`: an `ObservationExtractor` trait with a Phase-1 `LexiconExtractor` baseline (capitalised words + `@mentions` + `#tags` as entities, action verbs / `TODO` / `ACTION` / `TASK` as tasks, `decided` / `agreed` / `approved` as decisions, declarative sentences as facts) and an `ObservationPipeline` that chains extraction → reuse of the evidence-plane `ImportanceClassifier` → Candidate observation creation. New module `crates/evidence_store/src/retrieval.rs`: a `HybridRetriever` exposing `search_fts` (FTS5 lexical), `search_recency` (created-at decay), and `search_hybrid` (weighted fan-in over FTS5, recency, and a stubbed `0.0` semantic-vector score until XLM-R lands). 72 new tests cover every valid + invalid state-machine transition, pinning + decay scenarios, working-memory TTL eviction, the privacy-strip type-system invariant, lexicon extraction over entity / task / decision / fact inputs, end-to-end pipeline runs, retention scoring extremes, hybrid retrieval ordering with custom weights, and end-to-end memory + observation lifecycles. `cargo fmt`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all` (125 tests) all pass; the workspace clippy config relaxes `cast_precision_loss` and `float_cmp` (consistent with the existing cast allowances) to support the f64-based scoring path. |
| 2026-05-07 | **Phase 0 — Foundation, first delivery.** Landed the Cargo workspace at the repo root with three library crates (`crates/crypto`, `crates/evidence_store`, `crates/sync_engine`). Implemented the post-quantum `crypto` crate: BLAKE3 content hashing, XChaCha20-Poly1305 AEAD, HKDF-SHA256 key derivation, and a hybrid X25519 + ML-KEM-768 KEM with a concatenate-then-KDF combiner — both halves real, ML-KEM-768 via the RustCrypto `ml-kem` crate behind a swappable `KemBackend` trait. Implemented the SQLCipher-backed `evidence_store` crate: schema with `evidence` (append-only via triggers), `body_store` (BLAKE3-keyed dedup with `ref_count`), `ring_buffer` (FIFO eviction at a configurable cap, default 5 MB), and an `evidence_fts` FTS5 virtual table with the substrate-canonical `unicode61 remove_diacritics 2` tokenizer. Built the size-threshold storage router (inline `≤ 512 B`, body-table `> 512 B`, ring-buffer for noise) and the per-scope-keyed AEAD ingestion pipeline. Added a lexicon-only `ImportanceClassifier` trait + `LexiconClassifier` Phase-0 fallback with a configurable lexicon (default English chat lexicon). Added a 53-test suite covering hashing, AEAD round-trip + tamper-detection, KDF determinism, hybrid KEM round-trip, schema bootstrap, ingestion round-trip on every storage path, content-hash dedup ref-count, ring-buffer FIFO eviction + size-cap enforcement, FTS5 indexing + search, classifier behaviour, and the append-only invariant on `evidence`. Added `.github/workflows/ci.yml` running `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets`, and `cargo test --all` with cargo caching. Remaining Phase 0 items (SLM-backed importance classifier via Bonsai-1.7B, iOS / Android / macOS / Windows platform bindings) are out of scope for this delivery and tracked above. |
| 2026-05-06 | **Initial project setup.** Replaced the empty `README.md` and added `PROPOSAL.md`, `ARCHITECTURE.md`, `PHASES.md`, and `PROGRESS.md`. Documents define the privacy-first continual knowledge / context substrate (B2C + B2B over one platform), the layered six-plane substrate (evidence → observation → semantic → reasoning → export → action), the six-stage memory model with decay state machine, the strict knowledge hierarchy (`user → community → channel` for B2C, `user → domain → channel` per tenant for B2B), the on-device model strategy referencing [`kennguy3n/slm-chat-demo`](https://github.com/kennguy3n/slm-chat-demo) (Bonsai-1.7B + XLM-R + device tiering), the modified llama.cpp inference runtime via [`kennguy3n/llama.cpp@prism`](https://github.com/kennguy3n/llama.cpp/tree/prism), the post-quantum crypto layer (hybrid X25519 + ML-KEM-768 KEM, ML-DSA-65 + SPHINCS+ signatures, post-quantum MLS), the connector inventory (Google Drive, OneDrive, Notion, Jira, Confluence, Figma, HubSpot, Slack, email), the three deployment modes (local-only / enterprise server-side / confidential-compute hybrid), the Zanzibar-style relation graph + cryptographic capability permission model with proposal-only agent writes, and the seven-phase delivery plan with a 30 / 60 / 90-day implementation timeline targeting Phases 0 → 2. No implementation work has shipped yet — development begins with Phase 0. |

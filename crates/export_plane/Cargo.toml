[package]
name = "export_plane"
description = "Export plane for the Knowledge substrate — portable concept profiles, export policies, controls, and a read-only policy simulator."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
audit_service = { path = "../audit_service" }
concept_graph = { path = "../concept_graph" }
crypto = { path = "../crypto", package = "crypto" }
evidence_store = { path = "../evidence_store" }
memory_manager = { path = "../memory_manager" }

thiserror = { workspace = true }
uuid = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
agent_contract = { path = "../agent_contract" }

[lints]
workspace = true
